<div class="row">
	<div class="ui teal stackable four item menu">
		<%= nav_link("Orders", user_orders_url(@user)) %>
		<%= nav_link("Messages", "#") %>
		<%= nav_link("Edit Profile", edit_user_url(@user)) %>
		<%= nav_link("Delete Account", leave_user_url(@user)) %>
	</div>
</div>